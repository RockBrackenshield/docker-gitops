services:
  kopia:
    image: kopia/kopia:20260623.0.3052
    container_name: kopia-server
    restart: unless-stopped

    command:
      - server
      - start
      - --address=0.0.0.0:51515
      - --tls-cert-file=app/certs/kopia-server.crt
      - --tls-key-file=app/certs/kopia-server.key
      - --config-file=app/config/repository.config
      - --cache-directory=/app/cache
      - --no-check-for-updates

    environment:
      TZ: ${TZ:-America/Detroit}

    volumes:
      - /srv/homelab/kopia/config:/app/config
      - /srv/homelab/kopia/cache:/app/cache
      - /srv/homelab/kopia/logs:/app/logs
      - /srv/homelab/kopia/certs:/app/certs:ro

    healthcheck:
      test:
        - CMD
        - kopia
        - server
        - status
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

networks:
  traefik:
    external: true